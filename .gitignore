# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Lock files
yarn.lock
package-lock.json

# Build output
node_modules
dist
dist-ssr
*.local
*.tsbuildinfo

# Env files (contain MongoDB URI, Supabase keys, etc.)
.env
.env.local
.env.*.local

# Claude Code local overrides (per-machine, not shared)
.claude/settings.local.json

# Skill registry cache (regenerated, not source of truth)
.atl/.skill-registry.cache.json

# CodeGraph local index (per-machine, rebuilt via init)
.codegraph/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Next.js
/.next/
/out/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Local tooling caches
.codegraph/
.atl/.skill-registry.cache.json
